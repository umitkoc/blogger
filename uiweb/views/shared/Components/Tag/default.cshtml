@model ICollection<Tag>

@if(Model.Count()!=0){
    @foreach (var item in Model)
{
    <a asp-controller="home" asp-action="TagBlog" asp-route-id=@item.TagId class="badge bg-primary p-1">#@item.TagName</a>
}
}else{
       <div class="alert alert-danger">Not Tag</div>
}